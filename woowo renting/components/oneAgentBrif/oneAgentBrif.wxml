<view class="one-agent-container"
  bindtap="goToCompany"
  data-company-info="{{companyInfo}}">
  <view class="company-logo">
    <image class="company-log-img"
      src="{{roundLogoUrl}}" mode="aspectFill"></image>
  </view>
  <view class="company-name">{{companyInfo.name}}</view>
</view>